{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome",
      "request": "launch",
      "type": "chrome",
      "url": "http://localhost:9001",
      "webRoot": "${workspaceFolder}",
      "cwd": "${workspaceRoot}",
    }
  ]
}
